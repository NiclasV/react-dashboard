import React, { Component } from 'react';

class Header extends Component {
    render() {
        let Title = <h1>Dashboard UI</h1>;
        return (
        <header>
            {Title}
        </header>
        );
    }
}

export default Header; 